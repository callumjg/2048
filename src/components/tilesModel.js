const model = {
	addRandomTile: () => {
		return console.log("generating random tiles...");
	},

	moveTiles: direction => {
		console.log(direction);
	},

	getNewTilePosition: () => {},

	isAdjacentTileFree: () => {},

	orderTilesToGetMove: () => {},

	reOrderTiles: () => {},

	tileKeyCounter: 0
};

export default model;
